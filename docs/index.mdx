---
title: "Overview"
description: "Build, test, and ship MCP Apps to ChatGPT, Claude, and more with the sunpeak framework."
keywords: ["MCP Apps", "ChatGPT Apps", "MCP app framework", "ChatGPT app framework", "Claude Apps", "Claude Connectors", "sunpeak"]
---

import ProjectStructure from '/snippets/project-structure.mdx';
import Examples from '/snippets/examples.mdx';

<Info>
**Definitions**: An **MCP App** is an interactive UI embedded in an agent conversation.

A **ChatGPT App** is an **MCP App** with optional ChatGPT-specific features.

The UI of an MCP App is an **MCP Resource**.

The API of an MCP App is an **MCP Tool**.

More on MCP [here](/library/mcp-server). See the [MCP Apps protocol reference](/mcp-apps/introduction) for the underlying specification.
</Info>

<Tip>
This documentation is available via MCP at https://sunpeak.ai/docs/mcp, use it in your coding agent!
</Tip>

<Tip>
Install the `create-sunpeak-app` coding agent skill for built-in knowledge of sunpeak patterns, hooks, simulations, and testing conventions:

```bash
npx skills add Sunpeak-AI/sunpeak@create-sunpeak-app
```
</Tip>

<video
  src="https://cdn.sunpeak.ai/sunpeak-demo-prod.mp4"
  autoPlay
  muted
  loop
  playsInline
  controls
  className="w-full aspect-video rounded-xl"
></video>

## Overview

sunpeak is an npm package that helps you build MCP Apps — interactive UIs that run inside AI chat hosts like ChatGPT and Claude. sunpeak consists of:

### The sunpeak Library

1. **[Runtime APIs](/library/runtime-apis)**: Strongly typed APIs for the MCP Apps runtime, **architected for multi-platform support** (ChatGPT, Claude, and more).
2. **[Simulator](/library/simulator)**: React component replicating the host runtime to **test Apps locally and automatically**.
3. **[MCP Server](/library/mcp-server)**: Serve Resources with mock data to real hosts with HMR (**no more cache issues or 5-click manual refreshes**).

### The sunpeak Framework

Next.js for MCP Apps. Using a Review page as an example, sunpeak projects look like:

<ProjectStructure />

1. **[Project Scaffold](/template/project-scaffold)**: Complete development setup with the sunpeak library.
2. **[UI Components](/template/ui-components)**: Production-ready components following MCP App design guidelines.
3. **Convention over configuration**:
   - Create a UI by creating a `.tsx` file in `src/resources/{name}/` that exports a `ResourceConfig` and a React component ([example](#resource)).
   - Create a tool by creating a `.ts` file in `src/tools/` that exports `tool` (metadata with resource reference), `schema` (Zod), and a `default` handler ([example](#tool)).
   - Create test state (simulations) by creating a `.json` file in `tests/simulations/` ([example](#simulation)).

### The sunpeak CLI

Commands for managing MCP Apps:

- `sunpeak new` - Create a new project
- `sunpeak dev` - Start dev server with MCP endpoint and live simulator
- `sunpeak build` - Build resources for production
- `sunpeak upgrade` - Upgrade sunpeak to latest version

<Examples />
